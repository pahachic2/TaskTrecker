@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("login", true); // Force load to ensure state is cleared if needed
    }
}
